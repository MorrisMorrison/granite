# deploy — self-hosting assets

Will hold the `docker-compose.yml`, `.env.example`, and any deployment helpers. The whole backend is a
single Go binary + a SQLite file, so deployment is one container behind a reverse proxy.

> Not populated yet — lands in Phase 4. See [docs/07 — Self-hosting](../../docs/07-self-hosting.md).
