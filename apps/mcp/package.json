{
  "name": "@granite/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Model Context Protocol server for Granite (read tools + opt-in write tools over the REST API).",
  "bin": {
    "granite-mcp": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.18.0",
    "openapi-fetch": "^0.17.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@granite/shared": "workspace:*",
    "@types/node": "^22.10.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
